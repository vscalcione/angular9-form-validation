import { ValidateUserDirective } from './validate-user.directive';

describe('ValidateUserDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateUserDirective();
    expect(directive).toBeTruthy();
  });
});
